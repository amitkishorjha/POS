@model IEnumerable<POS.Models.RoleMaster>
@{
    ViewBag.Title = "Index";
}

<ol class="breadcrumb">
    <li class="breadcrumb-item"><a href="#">Master</a></li>
    <li class="breadcrumb-item active">Role</li>
</ol>
<hr />
<h2 class="page-title">
    Manage Roles
</h2>
<p>
    <a href="@Url.Action("Create", "RoleMaster")" class="btn btn-success btn-shadow">
        Create
        <span class="fa fa-plus-circle" aria-hidden="true"></span>
    </a>
</p>
<table id="RoleMaster" class="table table-striped table-bordered table-hover">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AccessCode)
            </th>
                Actions
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.AccessCode)
                </td>
                <td>
                    <div class="btn-group">
                        <button type="button" class="btn btn-danger">Action</button>
                        <button type="button" class="btn btn-danger dropdown-toggle" data-toggle="dropdown">
                            <span class="caret"></span>
                            <span class="sr-only">Toggle Dropdown</span>
                        </button>
                        <div class="dropdown-menu">
                            <a class="dropdown-item" href="@Url.Action("RoleActionMapping","RoleMaster")?UniqueId=@item.UniqueId">Action Mapping</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="@Url.Action("Edit","RoleMaster")?UniqueId=@item.UniqueId">Edit</a>
                            <a class="dropdown-item" href="@Url.Action("Details","RoleMaster")?UniqueId=@item.UniqueId">Details</a>
                            <a class="dropdown-item" href="@Url.Action("Delete","RoleMaster")?UniqueId=@item.UniqueId">Delete</a>
                        </div>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>
@section scripts{
    <script>
        $(document).ready(function () {
            $('#RoleMaster').DataTable();
        });
    </script>
}
